#include "main.h"

void _puts(char *str)

#include<stdio.h>

/**
 * main - Entry point
 * _puts - entry
 * Return: Always 0 (Success)
 */

int main(void)
{
	("\"Programming is like building a multilingual puzzle");
	return (0);
}
